REACT_APP_BACKEND_URL=http://localhost:8001
WDS_SOCKET_PORT=3000
# Docker / EasyPanel: you can set REACT_APP_BACKEND_URL (or BACKEND_URL) on the
# frontend container at runtime; it overrides the build-time value below.
